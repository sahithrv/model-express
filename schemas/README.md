# Schemas

Shared contracts for the orchestrator, worker, agents, and UI.

Initial schemas to add:

- project lifecycle API
- worker registration and heartbeat
- worker job polling and assignment response
- experiment job config
- epoch metric report
- pruning decision
- agent structured outputs
- memory insight record
- allocation mode request
- model export manifest
